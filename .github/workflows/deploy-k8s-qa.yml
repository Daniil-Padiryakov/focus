# ================================
# Deploy to Kubernetes (QA)
# ================================
# Triggers: Push to develop branch OR manual
# Target: QA/Testing Kubernetes cluster
# Purpose: Testing environment before production

name: Deploy to K8s QA

on:
  push:
    branches:
      - develop
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_TAG: develop

jobs:
  deploy:
    name: Deploy to QA Cluster
    runs-on: ubuntu-latest
    environment:
      name: qa
      url: https://qa.focus-app.ru

    steps:
      # ================================
      # Checkout
      # ================================
      - name: Checkout code
        uses: actions/checkout@v4

      # ================================
      # Setup Kubernetes
      # ================================
      - name: Setup kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: 'v1.28.0'

      - name: Configure kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.QA_KUBECONFIG }}" | base64 -d > ~/.kube/config
          chmod 600 ~/.kube/config

      - name: Verify cluster access
        run: |
          kubectl cluster-info
          kubectl get nodes

      # ================================
      # Deploy to Kubernetes
      # ================================
      - name: Deploy to Kubernetes
        run: |
          echo "🚀 Deploying to QA cluster..."

          # Apply all manifests (creates or updates resources)
          # This will automatically create namespace, configmaps, secrets, deployments
          kubectl apply -f k8s/base/

          echo "✅ Manifests applied"

      # ================================
      # Wait for rollout
      # ================================
      - name: Wait for backend rollout
        run: |
          kubectl rollout status deployment/backend -n focus --timeout=5m

      - name: Wait for frontend rollout
        run: |
          kubectl rollout status deployment/frontend -n focus --timeout=3m

      # ================================
      # Run migrations
      # ================================
      - name: Run database migrations
        run: |
          # Get first backend pod
          POD=$(kubectl get pod -n focus -l app=backend -o jsonpath='{.items[0].metadata.name}')

          # Run migrations
          kubectl exec -n focus $POD -- pnpm --filter @focus/backend db:migrate

      # ================================
      # Health checks
      # ================================
      - name: Check deployment health
        run: |
          echo "Checking backend health..."
          kubectl wait --for=condition=ready pod -l app=backend -n focus --timeout=120s

          echo "Checking frontend health..."
          kubectl wait --for=condition=ready pod -l app=frontend -n focus --timeout=60s

          echo "✅ All pods are healthy"

      - name: Verify services
        run: |
          kubectl get pods -n focus
          kubectl get svc -n focus
          kubectl get ingress -n focus

      # ================================
      # Rollback on failure
      # ================================
      - name: Rollback on failure
        if: failure()
        run: |
          echo "❌ Deployment failed, rolling back..."

          # Rollback backend
          kubectl rollout undo deployment/backend -n focus

          # Rollback frontend
          kubectl rollout undo deployment/frontend -n focus

          echo "✅ Rollback completed"

